import CircularProgress from "@material-ui/core/CircularProgress";
import React from "react";
import {ProgressRoot} from "./styles";

const Progress = () => {
		return (
				<ProgressRoot>
						<CircularProgress/>
				</ProgressRoot>
		)
}

export default Progress
